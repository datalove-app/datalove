//!

mod lib;
use lib::Clock;

struct PrivateInput {
    clock: Clock,
}

struct PublicInput {
    clock: bool,
}

fn main(input: PrivateInput) -> PublicInput {
    PublicInput {
        clock: false,
    }
}
